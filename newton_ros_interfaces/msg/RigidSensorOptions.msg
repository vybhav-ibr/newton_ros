# Attachment properties for a sensor on a rigid body
string link             # Name of the link to attach to
float64[3] pos_offset   # Position offset [x, y, z]
float64[3] euler_offset # Rotation offset [roll, pitch, yaw]
